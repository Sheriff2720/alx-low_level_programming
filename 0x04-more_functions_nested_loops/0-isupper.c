#include "main.h"

/**
 * _isupper - checks for uppercase
 *
 * @c: character to check for the case
 *
 * Return: 1 if upercase, 0 if not upercase
 */
int _isupper(int c)
{
	if (c >= 'A' && c <= 'Z')
		return (1);
	reurn (0);
}
